# APT Reports

Local APT audit outputs for `apt-novel-reviewer`.

- Canonical doctrine source: `../../../../apt-principles`
- Human-readable reports: `docs/apt/reports/*.md`
- Machine-readable outputs: `docs/apt/reports/static/*`

Keep doctrine changes in `apt-principles`. Keep only repo-specific findings and evidence here.
