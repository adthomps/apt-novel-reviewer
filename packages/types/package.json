{
  "name": "@apt/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "echo \"No tests for @apt/types\""
  }
}
